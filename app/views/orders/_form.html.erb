<%= form_for [@restaurant, @order], remote: true do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :note, "List any special notes for this order (e.g. allergies)" %><br>
    <%= f.text_field :note %>
  </div>
  <%= associated_fields(f, :ordered_items, @ordered_items) %>
  <%= link_to_add_fields "Add Another Item", f, :ordered_items %><br><br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
