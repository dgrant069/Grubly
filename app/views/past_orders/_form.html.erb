<%= form_for(@past_order) do |f| %>
  <% if @past_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@past_order.errors.count, "error") %> prohibited this past_order from being saved:</h2>

      <ul>
      <% @past_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
